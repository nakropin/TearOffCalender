import {renderPageCallCounter} from "/tearoffscript.js"

function handleClick( e ) {
        if( renderPageCallCounter < randomfiles.length ){
          updateCalendar( e.target );
        };
    };
export default handleClick;